# electron-builder config with environment variables
# This extends the base config in package.json

mac:
  # For distribution: set APPLE_IDENTITY env var to "Developer ID Application: Name (TEAMID)"
  # Without it, builds are unsigned - users must run: xattr -cr /Applications/1Code.app
  identity: null
